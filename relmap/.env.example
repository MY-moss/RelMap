# RelMap 环境变量配置示例
# 复制此文件为 .env 并填写实际值
# .env 文件已被 .gitignore 忽略，不会提交到版本控制

# Sentry 错误监控 DSN
# 从 Sentry 控制台获取：Project Settings -> Client Keys (DSN)
# 留空则禁用错误上报（开发环境默认不上报）
# 主进程通过 process.env.SENTRY_DSN 读取
# 渲染进程通过 import.meta.env.VITE_SENTRY_DSN 读取
SENTRY_DSN=
VITE_SENTRY_DSN=
